## what is database: dbms

## DBMS: Database Management System

A dtatabase is a structured collection of related data , organized , efficient storage , retrival and ,managmnet.

##### data:data is a fact that can be reorded in the from of text , number , image , audio or video.

##### information: information is processed data that is meaningful and useful for decision-making.

## Why File System is fail..?

ফাইল সিস্টেম দিয়ে ডেটা স্টোর করা যায় — এটা সত্যি।
কিন্তু জটিল বা বড় অ্যাপ্লিকেশন তৈরি করতে গেলে ফাইল সিস্টেম ব্যর্থ হয়ে যায়।
সেই কারণে আমাদের ডেটাবেস দরকার হয়।

❌ কেন File System ব্যর্থ হয়?

ফাইল সিস্টেমে ডেটা .txt, .json, .csv বা .xml ফাইল হিসেবে রাখা হয়।
কিন্তু এতে অনেক সমস্যা হয়:

1️⃣ ডেটা Search করতে অনেক সময় লাগে

ধরো তোমার কাছে ৫০ লাখ ইউজারের তথ্য একটি JSON ফাইলে আছে।
এখন তুমি শুধু "Id = 87456" ইউজারটা খুঁজতে চাও।

ফাইল সিস্টেমে:

পুরো ফাইল line-by-line স্ক্যান করতে হবে

এতে অনেক সময় লাগে

বড় ডেটা হলে সিস্টেম slow হয়ে যায়

➡️ ডেটাবেসে index থাকে, তাই মিলিসেকেন্ডে খুঁজে পাওয়া যায়।

2️⃣ ফাইল লকিং সমস্যা (Concurrency Problem)

ধরো একই সময়ে ১০০ জন ইউজার ডেটা আপডেট করছে।

ফাইল সিস্টেমে:

একটি ফাইল এক সময়ে ১–২ প্রোগ্রামই লেখতে পারে

ফাইল লক হয়ে গেলে অন্য ইউজার অপেক্ষা করতে থাকে

এতে deadlock, data loss, corruption হয়

➡️ ডেটাবেসে হাজার-লাখ concurrent write/read possible (ACID)।

4️⃣ Relationship handle করতে পারে না

ধরো তোমার ২টি ডেটা আছে:

Students

Departments

এদের মধ্যে relation (CSE -> Students) আছে।
ফাইল সিস্টেমে relation maintain করতে হলে manually লাফাতে হবে
এবং ভুল হওয়ার সম্ভাবনা খুব বেশি।

➡️ Relational Database table & keys দিয়ে relation maintain করে।

5️⃣ Data Update এবং Delete করা কঠিন

ধরো তুমি একটি JSON ফাইল থেকে কোনো ইউজার মুছতে চাও।
তাহলে পুরো ফাইল rewrite করতে হয়—
বড় ফাইল হলে সময় বেশি লাগে।

➡️ ডেটাবেসে UPDATE এবং DELETE খুব দ্রুত।

6️⃣ Security নেই

ফাইল সিস্টেমে:

file read করলে সব ডেটা দেখা যায়

fine-grained access control নেই

কোনো audit নেই

➡️ ডেটাবেসে roles, permissions, encryption, auditing থাকে।

7️⃣ Backup, Recovery, Replication হয় না

ফাইল সিস্টেমে ক্ষতি হলে সব ডেটা উড়তে পারে।

➡️ ডেটাবেস নিজে থেকে backup, replication, failover সাপোর্ট করে।

✔️ তাই ডেটাবেস প্রয়োজন — কেন?

| সমস্যা       | ডেটাবেস কীভাবে সমাধান করে |
| ------------ | ------------------------- |
| Slow Search  | Index, Query Optimization |
| Concurrency  | ACID, Locking System      |
| Corruption   | Transactions              |
| Relationship | Foreign Keys, JOINS       |
| Slow Update  | Efficient Write Engine    |
| No Security  | Roles, Permissions        |
| Backup Issue | Auto backup, recovery     |

ডেটাবেস ব্যবহারের মাধ্যমে এই সব সমস্যার সমাধান হয় এবং ডেটা ম্যানেজমেন্ট অনেক সহজ ও কার্যকর হয়। তাই বড় ও জটিল অ্যাপ্লিকেশন তৈরির জন্য ডেটাবেস অপরিহার্য।

## Types of DBMS

1.Relational Database Management System (RDBMS)-->MySQL, PostgreSQL, Oracle DB, SQL Server
2.NoSQL Database Management System--->Mongodb, Cassandra, Redis
3.Object-Oriented Database Management System (OODBMS) --->db4o, ObjectDB
4.key-Value Store--->Redis, DynamoDB
5.Graph Database Management System--->Neo4j, Amazon Neptune

## ⭐ ডেটাবেজ মডেল (Database Model) কী?

ডেটাবেজ মডেল হলো—
ডেটা কীভাবে স্টোর হবে, কীভাবে সাজানো থাকবে, এবং ডেটাগুলোর মধ্যে সম্পর্ক কীভাবে থাকবে — তার নিয়ম বা স্ট্রাকচার।

সহজভাবে:
ডেটাবেজ ডিজাইনের “নকশা” বা “স্ট্রাকচার” = Database Model.

⭐ ডেটাবেজ মডেলের ধরন (Types of Database Models)

মূলত ৫ ধরনের ডেটাবেজ মডেল সবচেয়ে বেশি ব্যবহৃত হয়:

1️⃣ Hierarchical Model
2️⃣ Network Model
3️⃣ Relational Model (RDBMS)
4️⃣ Object-Oriented Model (OODBMS)
5️⃣ NoSQL Models

⭐ 1. Hierarchical Database Model

(হায়ারার্কিকাল বা “গাছের মতো” স্ট্রাকচার)

📌 ব্যাখ্যা:

ডেটা থাকে Tree Structure আকারে

একটিই parent → একাধিক child

Parent ছাড়া child থাকতে পারে না

             Company
               |
         ----------------
         |      |      |
      HR Dept  IT Dept Finance Dept

📌 কোথায় ব্যবহৃত হয়:

পুরনো সিস্টেম

File System design

কিছু banking system

⭐ 2. Network Database Model

(হায়ারার্কির উন্নত সংস্করণ)

📌 ব্যাখ্যা:

একাধিক parent-child relationship থাকে

Data many-to-many connect করা যায়

📌 উদাহরণ:

একজন Student একাধিক course নিতে পারে
একটি course-এ অনেক student থাকতে পারে

📌 ব্যবহারের জায়গা:

পুরনো telecom system

পুরনো inventory system

এখন খুব কম ব্যবহৃত হয়।

⭐ 3. Relational Database Model (RDBMS)

(সবচেয়ে জনপ্রিয় মডেল)

📌 ব্যাখ্যা:

ডেটা টেবিলে রাখা হয় (Row + Column)

টেবিলগুলোর মধ্যে সম্পর্ক (Relation) থাকে

SQL ব্যবহার করা হয়

| id  | name | dept |
| --- | ---- | ---- |

📌 যেখানে ব্যবহার হয়:

Facebook

E-commerce

Banking

University systems

📌 উদাহরণ সফটওয়্যার:

MySQL

PostgreSQL

Oracle

SQL Server

এটাই আজকের 80% সিস্টেমে ব্যবহৃত মডেল।

⭐ 4. Object-Oriented Database Model (OODBMS)
📌 ব্যাখ্যা:

ডেটা থাকে Objects আকারে

OOP concept ব্যবহার করা হয়

Class, Object, Inheritance ইত্যাদি follow করে

📌 ব্যবহারের জায়গা:

Real-time systems

Game development

Complex simulations

📌 উদাহরণ:

db4o

ObjectDB

⭐ 5. NoSQL Database Models

(SQL নয়, flexible data structure)

NoSQL-এর ৪টি সাব-মডেল আছে:

5.1 Document Model

ডেটা স্টোর হয় JSON-এর মতো ডকুমেন্টে।

{
"name": "Alamin",
"age": 22,
"skills": ["JS", "Node"]
}

কোথায় ব্যবহার:

E-commerce

Social media

Real-time apps

ডেটাবেজ উদাহরণ:

MongoDB

CouchDB

5.2 Key-Value Model

ডেটা store হয় key-value pair আকারে।

user123 → {name: "Alamin", age: 22}

ডেটাবেজ:

Redis

DynamoDB

5.3 Column Model

Rows নয়, data রাখা হয় columns অনুযায়ী।

ডেটাবেজ:

Cassandra

HBase

ব্যবহারের জায়গা:

Very large-scale data

Big Data systems

5.4 Graph Model

ডেটা থাকে নোড + এজ আকারে
(Relationship-heavy systems)

উদাহরণ:

Social network

Recommendation system

ডেটাবেজ:

Neo4j

Amazon Neptune
