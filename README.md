## what is database: dbms

## DBMS: Database Management System

A dtatabase is a structured collection of related data , organized , efficient storage , retrival and ,managmnet.

##### data:data is a fact that can be reorded in the from of text , number , image , audio or video.

##### information: information is processed data that is meaningful and useful for decision-making.

## Why File System is fail..?

ফাইল সিস্টেম দিয়ে ডেটা স্টোর করা যায় — এটা সত্যি।
কিন্তু জটিল বা বড় অ্যাপ্লিকেশন তৈরি করতে গেলে ফাইল সিস্টেম ব্যর্থ হয়ে যায়।
সেই কারণে আমাদের ডেটাবেস দরকার হয়।

❌ কেন File System ব্যর্থ হয়?

ফাইল সিস্টেমে ডেটা .txt, .json, .csv বা .xml ফাইল হিসেবে রাখা হয়।
কিন্তু এতে অনেক সমস্যা হয়:

1️⃣ ডেটা Search করতে অনেক সময় লাগে

ধরো তোমার কাছে ৫০ লাখ ইউজারের তথ্য একটি JSON ফাইলে আছে।
এখন তুমি শুধু "Id = 87456" ইউজারটা খুঁজতে চাও।

ফাইল সিস্টেমে:

পুরো ফাইল line-by-line স্ক্যান করতে হবে

এতে অনেক সময় লাগে

বড় ডেটা হলে সিস্টেম slow হয়ে যায়

➡️ ডেটাবেসে index থাকে, তাই মিলিসেকেন্ডে খুঁজে পাওয়া যায়।

2️⃣ ফাইল লকিং সমস্যা (Concurrency Problem)

ধরো একই সময়ে ১০০ জন ইউজার ডেটা আপডেট করছে।

ফাইল সিস্টেমে:

একটি ফাইল এক সময়ে ১–২ প্রোগ্রামই লেখতে পারে

ফাইল লক হয়ে গেলে অন্য ইউজার অপেক্ষা করতে থাকে

এতে deadlock, data loss, corruption হয়

➡️ ডেটাবেসে হাজার-লাখ concurrent write/read possible (ACID)।

4️⃣ Relationship handle করতে পারে না

ধরো তোমার ২টি ডেটা আছে:

Students

Departments

এদের মধ্যে relation (CSE -> Students) আছে।
ফাইল সিস্টেমে relation maintain করতে হলে manually লাফাতে হবে
এবং ভুল হওয়ার সম্ভাবনা খুব বেশি।

➡️ Relational Database table & keys দিয়ে relation maintain করে।

5️⃣ Data Update এবং Delete করা কঠিন

ধরো তুমি একটি JSON ফাইল থেকে কোনো ইউজার মুছতে চাও।
তাহলে পুরো ফাইল rewrite করতে হয়—
বড় ফাইল হলে সময় বেশি লাগে।

➡️ ডেটাবেসে UPDATE এবং DELETE খুব দ্রুত।

6️⃣ Security নেই

ফাইল সিস্টেমে:

file read করলে সব ডেটা দেখা যায়

fine-grained access control নেই

কোনো audit নেই

➡️ ডেটাবেসে roles, permissions, encryption, auditing থাকে।

7️⃣ Backup, Recovery, Replication হয় না

ফাইল সিস্টেমে ক্ষতি হলে সব ডেটা উড়তে পারে।

➡️ ডেটাবেস নিজে থেকে backup, replication, failover সাপোর্ট করে।

✔️ তাই ডেটাবেস প্রয়োজন — কেন?

| সমস্যা       | ডেটাবেস কীভাবে সমাধান করে |
| ------------ | ------------------------- |
| Slow Search  | Index, Query Optimization |
| Concurrency  | ACID, Locking System      |
| Corruption   | Transactions              |
| Relationship | Foreign Keys, JOINS       |
| Slow Update  | Efficient Write Engine    |
| No Security  | Roles, Permissions        |
| Backup Issue | Auto backup, recovery     |

ডেটাবেস ব্যবহারের মাধ্যমে এই সব সমস্যার সমাধান হয় এবং ডেটা ম্যানেজমেন্ট অনেক সহজ ও কার্যকর হয়। তাই বড় ও জটিল অ্যাপ্লিকেশন তৈরির জন্য ডেটাবেস অপরিহার্য।

## Types of DBMS

1.Relational Database Management System (RDBMS)-->MySQL, PostgreSQL, Oracle DB, SQL Server
2.NoSQL Database Management System--->Mongodb, Cassandra, Redis
3.Object-Oriented Database Management System (OODBMS) --->db4o, ObjectDB
4.key-Value Store--->Redis, DynamoDB
5.Graph Database Management System--->Neo4j, Amazon Neptune

## ⭐ ডেটাবেজ মডেল (Database Model) কী?

ডেটাবেজ মডেল হলো—
ডেটা কীভাবে স্টোর হবে, কীভাবে সাজানো থাকবে, এবং ডেটাগুলোর মধ্যে সম্পর্ক কীভাবে থাকবে — তার নিয়ম বা স্ট্রাকচার।

সহজভাবে:
ডেটাবেজ ডিজাইনের “নকশা” বা “স্ট্রাকচার” = Database Model.

⭐ ডেটাবেজ মডেলের ধরন (Types of Database Models)

মূলত ৫ ধরনের ডেটাবেজ মডেল সবচেয়ে বেশি ব্যবহৃত হয়:

1️⃣ Hierarchical Model
2️⃣ Network Model
3️⃣ Relational Model (RDBMS)
4️⃣ Object-Oriented Model (OODBMS)
5️⃣ NoSQL Models

⭐ 1. Hierarchical Database Model

(হায়ারার্কিকাল বা “গাছের মতো” স্ট্রাকচার)

📌 ব্যাখ্যা:

ডেটা থাকে Tree Structure আকারে

একটিই parent → একাধিক child

Parent ছাড়া child থাকতে পারে না

             Company
               |
         ----------------
         |      |      |
      HR Dept  IT Dept Finance Dept

📌 কোথায় ব্যবহৃত হয়:

পুরনো সিস্টেম

File System design

কিছু banking system

⭐ 2. Network Database Model

(হায়ারার্কির উন্নত সংস্করণ)

📌 ব্যাখ্যা:

একাধিক parent-child relationship থাকে

Data many-to-many connect করা যায়

📌 উদাহরণ:

একজন Student একাধিক course নিতে পারে
একটি course-এ অনেক student থাকতে পারে

📌 ব্যবহারের জায়গা:

পুরনো telecom system

পুরনো inventory system

এখন খুব কম ব্যবহৃত হয়।

⭐ 3. Relational Database Model (RDBMS)

(সবচেয়ে জনপ্রিয় মডেল)

📌 ব্যাখ্যা:

ডেটা টেবিলে রাখা হয় (Row + Column)

টেবিলগুলোর মধ্যে সম্পর্ক (Relation) থাকে

SQL ব্যবহার করা হয়

| id  | name | dept |
| --- | ---- | ---- |

📌 যেখানে ব্যবহার হয়:

Facebook

E-commerce

Banking

University systems

📌 উদাহরণ সফটওয়্যার:

MySQL

PostgreSQL

Oracle

SQL Server

এটাই আজকের 80% সিস্টেমে ব্যবহৃত মডেল।

⭐ 4. Object-Oriented Database Model (OODBMS)
📌 ব্যাখ্যা:

ডেটা থাকে Objects আকারে

OOP concept ব্যবহার করা হয়

Class, Object, Inheritance ইত্যাদি follow করে

📌 ব্যবহারের জায়গা:

Real-time systems

Game development

Complex simulations

📌 উদাহরণ:

db4o

ObjectDB

⭐ 5. NoSQL Database Models

(SQL নয়, flexible data structure)

NoSQL-এর ৪টি সাব-মডেল আছে:

5.1 Document Model

ডেটা স্টোর হয় JSON-এর মতো ডকুমেন্টে।

{
"name": "Alamin",
"age": 22,
"skills": ["JS", "Node"]
}

কোথায় ব্যবহার:

E-commerce

Social media

Real-time apps

ডেটাবেজ উদাহরণ:

MongoDB

CouchDB

5.2 Key-Value Model

ডেটা store হয় key-value pair আকারে।

user123 → {name: "Alamin", age: 22}

ডেটাবেজ:

Redis

DynamoDB

5.3 Column Model

Rows নয়, data রাখা হয় columns অনুযায়ী।

ডেটাবেজ:

Cassandra

HBase

ব্যবহারের জায়গা:

Very large-scale data

Big Data systems

5.4 Graph Model

ডেটা থাকে নোড + এজ আকারে
(Relationship-heavy systems)

উদাহরণ:

Social network

Recommendation system

ডেটাবেজ:

Neo4j

Amazon Neptune

![alt text](image.png)

## ⭐ Database Table Anatomy (টেবিলের গঠন)

একটি টেবিল হচ্ছে ডেটা স্টোর করার একটি সংগঠিত কাঠামো।
এটাতে সাধারণত থাকে:

1️⃣ Table Name
2️⃣ Column / Field
3️⃣ Row / Record / Tuple
4️⃣ Primary Key (PK)
5️⃣ Foreign Key (FK)
6️⃣ Data Types
7️⃣ Constraints
8️⃣ Index
9️⃣ NULL vs NOT NULL
🔟 Default Values

🔶 1. Table Name (টেবিলের নাম)

এটা হলো টেবিলের পরিচয় বা নাম।

উদাহরণ:

users

products

orders

🔶 2. Column / Field (কলাম বা ফিল্ড)

কলাম মানে হলো—
ডেটার ক্যাটাগরি বা ফিচার।

উদাহরণ:

id

name

age

department

এগুলো প্রত্যেকটা হলো টেবিলের কলাম / attribute।

🔶 3. Row / Record / Tuple (সারি বা একটি সম্পূর্ণ ডেটা)

Row/Record = টেবিলের একটি সম্পূর্ণ ডেটা।

| id  | name   | age |
| --- | ------ | --- |
| 1   | Alamin | 22  |

🔶 4. Primary Key (PK) — প্রধান চাবি

Primary Key হলো এমন একটি কলাম যেটা:

✔ Unqiue
✔ Null হতে পারে না
✔ প্রত্যেক রেকর্ডকে আলাদা চিনায়

উদাহরণ:

user_id

product_id

এটা টেবিলের “National ID Card”-এর মতো।

🔶 5. Foreign Key (FK) — সম্পর্ক তৈরির চাবি

Foreign Key দিয়ে দুই টেবিলের মধ্যে সম্পর্ক তৈরি হয়।

উদাহরণ:
students(dept_id) → departments(id)

মানে students টেবিলের ডিপার্টমেন্ট কোন টেবিল থেকে এসেছে— সেটা FK দিয়ে বোঝানো হয়।

🔶 6. Data Types (ডেটার ধরণ)

প্রত্যেক কলামের ডেটা টাইপ থাকে:
| Data Type | Example |
| --------- | ---------- |
| INT | বয়স, আইডি |
| VARCHAR | নাম |
| DATE | জন্ম তারিখ |
| BOOLEAN | true/false |
| FLOAT | দাম |

🔶 7. Constraints (শর্ত)

Constraints মানে টেবিলের ডেটার উপর নিয়ম।

| Constraint  | মানে                     |
| ----------- | ------------------------ |
| PRIMARY KEY | ইউনিক ও নাল নয়           |
| FOREIGN KEY | অন্য টেবিলের রেফারেন্স   |
| UNIQUE      | ডুপ্লিকেট নয়             |
| NOT NULL    | ফিল্ড ফাঁকা রাখা যাবে না |
| CHECK       | শর্ত চেক করবে (age > 0)  |
| DEFAULT     | ডিফল্ট ভ্যালু            |

🔶 8. Index (ইন্ডেক্স)

Index হলো ডেটা দ্রুত খুঁজে বের করার সিস্টেম।

✔ সার্চ দ্রুত
✔ ফিল্টার দ্রুত

উদাহরণ:
CREATE INDEX idx_name ON users(name);

🔶 9. NULL vs NOT NULL

NULL = কোনো ভ্যালু নেই (empty)

NOT NULL = ফাঁকা রাখা যাবে না

| কলাম                     | মানে              |
| ------------------------ | ----------------- |
| name VARCHAR NOT NULL    | নাম must দিতে হবে |
| middle_name VARCHAR NULL | দিতে নাও পারো     |

🔶 10. Default Value (ডিফল্ট মান)

ডেটা না দিলে ডেটাবেজ নিজে থেকে মান বসাবে।

উদাহরণ:
status VARCHAR DEFAULT 'active'

অর্থ: status না দিলে অটো active সেট হবে।

![alt text](image-1.png)

⭐ 1. Degree of a Table (ডিগ্রি)

ডিগ্রি = টেবিলের মোট কলামের সংখ্যা

মানে, একটি টেবিলে যতগুলো column/field থাকে, সেই সংখ্যা হলো টেবিলের Degree।

| id  | name | age | dept |
| --- | ---- | --- | ---- |

এখানে কলাম = 4টি
👉 তাই Degree = 4

⭐ 2. Cardinality of a Table (কার্ডিনালিটি)

কার্ডিনালিটি = টেবিলের মোট রো এর সংখ্যা

মানে, টেবিলে যতগুলো row/record/tuple থাকে, সেই সংখ্যা হলো টেবিলের Cardinality।

| id  | name | age | dept |
| --- | ---- | --- | ---- |
| 1   | A    | 20  | CSE  |
| 2   | B    | 22  | EEE  |
| 3   | C    | 21  | CSE  |
| 4   | D    | 23  | BBA  |
| 5   | E    | 24  | LAW  |

এখানে Row = 5টি
👉 তাই Cardinality = 5

## ⭐ What is a Key in Database? (১ লাইনে সংজ্ঞা)

Key হলো এমন একটি অ্যাট্রিবিউট (কলাম) বা অ্যাট্রিবিউটের সেট যেগুলো ডেটাবেজে রেকর্ডকে ইউনিকভাবে চিনতে, খুঁজতে, এবং সম্পর্ক তৈরি করতে ব্যবহৃত হয়।

অর্থাৎ— Key ডেটাবেজকে সঠিক, পরিষ্কার এবং ইউনিক রাখে।

🔥 Types of Keys (সব Key একসাথে তালিকা)

1️⃣ Super Key
2️⃣ Candidate Key
3️⃣ Primary Key
4️⃣ Alternate Key
5️⃣ Composite Key
6️⃣ Foreign Key
7️⃣ Unique Key

⭐ 1. Super Key (সুপার কি)

Super Key = এমন যে কোনো কলাম বা কলামের কম্বিনেশন যা দিয়ে রেকর্ডকে ইউনিকভাবে চেনা যায়।

✔ এটি ইউনিক
✔ কিন্তু অতিরিক্ত (extra) attributes থাকতে পারে

| id  | name   | email                             | phone |
| --- | ------ | --------------------------------- | ----- |
| 1   | Alamin | [a@gmail.com](mailto:a@gmail.com) | 123   |
| 2   | Karim  | [k@gmail.com](mailto:k@gmail.com) | 456   |

Super Key-গুলোর উদাহরণ:

{id}

{email}

{phone}

{id, name}

{id, phone}

{email, phone}

{id, name, email} — (extra column থাকলেও super key হতে পারে)

👉 মূল কথা:
যে কোনো combination যা রেকর্ডকে ইউনিক করে = Super Key।

⭐ 2. Candidate Key (সেরা পছন্দ)

Candidate Keys = Super Key যেগুলো মিনিমাম (সবচেয়ে ছোট) থাকে।
অতিরিক্ত কোনো কলাম থাকে না।

আমাদের Example থেকে Candidate Keys:

{id}

{email}

{phone}

কারণ এগুলো কোনোটিই Extra column ছাড়া ইউনিক।

👉 Every Candidate Key is a Super Key
but
Not every Super Key is a Candidate Key.

⭐ 4. Alternate Key

Candidate Key → যেটা Primary Key হিসেবে নির্বাচিত হয়নি।

উদাহরণ:

Candidate Keys: id, email, phone
Primary Key: id
Alternate Keys: email, phone

⭐ 5. Composite Key

দুই বা তার বেশি কলাম মিলে একসাথে Primary Key বা Candidate Key তৈরি করলে তাকে Composite Key বলে।

উদাহরণ:

| course_id | student_id | marks |

এখানে প্রতিটি row কে {course_id + student_id} দিয়ে চেনা যায়।

Composite Key = {course_id, student_id}

⭐ 6. Foreign Key (FK)

Foreign Key হলো এমন একটি key যা অন্য টেবিলের Primary Key-কে রেফার করে।

উদাহরণ:

Students(dept_id) → Departments(id)

FK সম্পর্ক তৈরি করতে ব্যবহৃত হয়।

⭐ 7. Unique Key

Primary Key-এর মতোই,
✔ Unique
কিন্তু
❌ Null allowed (in most DBs)

উদাহরণ:
email UNIQUE
